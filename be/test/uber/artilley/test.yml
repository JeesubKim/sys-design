config:
  target: "http://localhost:8080"
  phases:
    - duration: 30		# 지속시간
      # arrivalRate: 1157 	# 초당 요청수
      arrivalRate: 200 	# 초당 요청수
scenarios:
  - name: Location
    flow:
      - get:
          url: "http://localhost:8080/api/v1/location/4"




