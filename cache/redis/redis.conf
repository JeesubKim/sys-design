# 포트 설정
port 6379

# 지속성 설정 (AOF 활성화)
appendonly yes

# 최대 메모리 사용량 설정 (예: 100MB)
maxmemory 100mb

# 메모리가 초과될 경우 오래된 키 삭제 (LRU 방식)
maxmemory-policy allkeys-lru

# 로그 레벨 설정 (debug | verbose | notice | warning)
loglevel notice
